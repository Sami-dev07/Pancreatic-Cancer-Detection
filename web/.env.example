# Base URL of your FastAPI backend (include trailing slash recommended for path joins)
# Local example:
# VITE_API_BASE_URL=http://127.0.0.1:8000/
#
# If unset, the app uses a mock prediction in development only (see src/services/api.ts).
VITE_API_BASE_URL=
